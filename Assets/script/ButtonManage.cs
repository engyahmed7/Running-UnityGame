using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

public class ButtonManage : MonoBehaviour
{
    public Button Button;
    public PlayerControl manager;
    public policefollow script;
    //Start is called before the first frame update
    void Start()
    {
    //    Button = GetComponent<Button>();
    //    manager = GameObject.Find("Player").GetComponent<PlayerControl>();
    //    script = GameObject.Find("police").GetComponent<policefollow>();
    //    Button.onClick.AddListener(setButton);
    //    Button.gameObject.SetActive(true);
    //}

    ////Update is called once per frame
    //void Update()
    //{

    //}
    //void setButton()
    //{
    //    manager.startGame();
    //    script.GameStart();
    }
}
